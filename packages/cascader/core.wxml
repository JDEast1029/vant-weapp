<mc-popup
  show="{{ isActive }}"
  round
  position="bottom"
  bind:close="handleClose"
>
     <view wx:if="showToolbar" class="vcm-picker-popup__header">
        <view 
            wx:if="cancelText" 
            class="vcm-picker-popup__item is-left"
            bindtap="handleCancel"
            data-type="cancel"
        >{{ cancelText }}</view>

        <!-- title -->
        <view 
            class="vcm-picker-popup__item is-title"
        >{{ title }}</view>

        <view 
            wx:if="okText" 
            class="vcm-picker-popup__item is-right" 
            bindtap="handleOk"
            data-type="ok"
        >{{ okText }}</view>
    </view>
    <mc-view 
        dataSource="{{ dataSource }}"
        loadData="{{ loadData }}"
        changeOnSelect="{{ changeOnSelect }}"></mv-view>
</mc-popup>